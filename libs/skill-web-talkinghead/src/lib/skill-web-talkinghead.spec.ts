import { skillWebTalkinghead } from './skill-web-talkinghead';

describe('skillWebTalkinghead', () => {
  it('should work', () => {
    expect(skillWebTalkinghead()).toEqual('skill-web-talkinghead');
  });
});
